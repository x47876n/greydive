import '../assets/styles/greydive.css';

const Menu = () => {
  return;
  <div>
    <button
      class="btn cursor"
      type="button"
      onClick={() => (window.location.href = '/greydive/#/viamo/testeador-1')}
    >
      Viamo
    </button>
    <button
      class="btn cursor"
      type="button"
      onClick={() => (window.location.href = '/greydive/#/nebula/testeador-2')}
    >
      Nebula
    </button>
  </div>;
};

export default Menu;
